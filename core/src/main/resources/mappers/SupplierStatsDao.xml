<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"        
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.go2plus.core.userCenter.dao.SupplierStatsDao">
  <!-- 多表连接示例，供大家参考 -->
  <!-- 多表连接示例，供大家参考 -->

  <select id="getHighlightLimitByUserId" resultType="java.lang.Integer">
      select highlight_limit from supplier_stats where user_id=#{userId}
  </select>
</mapper>
